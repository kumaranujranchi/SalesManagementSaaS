# Netlify Environment Variables Template
# Copy these values to your Netlify dashboard → Site settings → Environment variables

# Database Configuration (Required)
DATABASE_URL=postgresql://username:password@hostname/database?sslmode=require

# Session Security (Required - Generate a secure random string)
SESSION_SECRET=your_super_secret_session_key_change_this_to_random_string_32_chars_minimum

# Application Configuration (Required)
NODE_ENV=production

# Optional: Email Configuration (for user invitations)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Custom domain configuration
# BASE_DOMAIN=yourdomain.com

# Optional: File upload configuration
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=uploads
